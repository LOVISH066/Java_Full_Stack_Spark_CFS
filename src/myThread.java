class myThread extends Thread{

    // we have to give thread a job or task
    @Override // here we are using override becasue child clas sis not satify with parent class so we use overide\
    public void run() {
        // cpntext switvching 

        // tghread herechy usme main thread jo main method run karegya
        // jo line rad karegya in main method that is main thread// t,start then you have tom ka enaothe rthread
        // t.start then we say child of main thread
        // thread shecduler
        // ts is a compent tahtd ecideh which thread will work first
        // t.srtart = new thread will be creatde, and run the task executing run method
        
        
    }
}
